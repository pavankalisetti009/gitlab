{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "ActiveContext collections options",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "queue_shard_count": {
      "type": [
        "integer",
        "null"
      ],
      "minimum": 1,
      "description": "The number of parallel queues that a collection queue can be sharded."
    },
    "queue_shard_limit": {
      "type": [
        "integer",
        "null"
      ],
      "minimum": 1,
      "description": "For bulk processing queues: the batch size or number of queued items to process at a time."
    }
  }
}
