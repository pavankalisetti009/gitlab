{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Holds secrets manager settings",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "project_secrets_limit": {
      "type": "integer",
      "minimum": 0,
      "default": 100,
      "description": "Maximum number of secrets allowed per project in Secrets Manager. Set to 0 for unlimited."
    },
    "group_secrets_limit": {
      "type": "integer",
      "minimum": 0,
      "default": 500,
      "description": "Maximum number of secrets allowed per group in Secrets Manager. Set to 0 for unlimited."
    }
  }
}
