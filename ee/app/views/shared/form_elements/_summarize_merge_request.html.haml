- if can?(current_user, :summarize_new_merge_request, @project)
  .js-summarize-code-changes{ data: { project_id: @project.id, source_branch: @merge_request.source_branch, target_branch: @merge_request.target_branch } }
