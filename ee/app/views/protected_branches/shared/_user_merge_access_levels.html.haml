- user_merge_access_levels = protected_branch.merge_access_levels.for_user
- if user_merge_access_levels.any?
  %p.small
    = _('The following users can also merge into this branch: %{branch}') % { branch: user_merge_access_levels.map(&:humanize).to_sentence }
