- group_merge_access_levels = protected_branch.merge_access_levels.for_group
- if group_merge_access_levels.any?
  %p.small
    = _('Members of these groups can also merge into this branch: %{branch}') % { branch: group_merge_access_levels.map(&:humanize).to_sentence }
