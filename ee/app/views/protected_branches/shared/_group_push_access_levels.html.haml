- group_push_access_levels = protected_branch.push_access_levels.for_group
- if group_push_access_levels.any?
  %p.small
    = _('Members of these groups can also push to this branch: %{branch}') % { branch: group_push_access_levels.map(&:humanize).to_sentence }
