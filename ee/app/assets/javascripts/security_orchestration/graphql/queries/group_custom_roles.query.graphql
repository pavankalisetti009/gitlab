query groupCustomRoles($fullPath: ID!) {
  group(fullPath: $fullPath) {
    id
    memberRoles {
      nodes {
        id
        name
        enabledPermissions {
          edges {
            node {
              value
            }
          }
        }
      }
    }
    __typename
  }
}
