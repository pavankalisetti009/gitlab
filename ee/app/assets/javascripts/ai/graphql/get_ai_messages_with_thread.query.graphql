query getAiMessagesWithThread($threadId: AiConversationThreadID!) {
  aiMessages(threadId: $threadId) {
    nodes {
      id
      requestId
      content
      contentHtml
      errors
      errorCode
      role
      timestamp
      extras {
        sources
        hasFeedback
      }
    }
  }
}
