query searchGroups($search: String!, $topLevelOnly: Boolean, $parentPath: ID) {
  groups(search: $search, topLevelOnly: $topLevelOnly, parentPath: $parentPath) {
    nodes {
      id
      name
      fullPath
    }
  }
}
