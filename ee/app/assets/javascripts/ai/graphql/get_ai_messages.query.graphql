query getAiMessages($conversationType: AiConversationsThreadsConversationType) {
  aiMessages(conversationType: $conversationType) {
    nodes {
      id
      requestId
      content
      contentHtml
      errors
      errorCode
      role
      timestamp
      extras {
        sources
        hasFeedback
      }
    }
  }
}
