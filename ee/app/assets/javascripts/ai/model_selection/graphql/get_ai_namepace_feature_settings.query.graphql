query getAiNamespaceFeatureSettings($groupId: GroupID!) {
  aiModelSelectionNamespaceSettings(groupId: $groupId) {
    nodes {
      feature
      title
      mainFeature
      selectedModel {
        ref
        name
      }
      selectableModels {
        ref
        name
      }
    }
  }
}
