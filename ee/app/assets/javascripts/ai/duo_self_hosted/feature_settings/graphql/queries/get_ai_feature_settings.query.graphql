query getAiFeatureSetting {
  aiFeatureSettings {
    nodes {
      feature
      title
      mainFeature
      provider
      releaseState
      selfHostedModel {
        id
        releaseState
      }
      defaultGitlabModel {
        name
        ref
        modelProvider @gl_introduced(version: "18.6.0")
      }
      gitlabModel {
        name
        ref
        modelProvider @gl_introduced(version: "18.6.0")
      }
      validModels {
        nodes {
          id
          name
          model
          modelDisplayName
          releaseState
        }
      }
      validGitlabModels {
        nodes {
          name
          ref
          modelProvider @gl_introduced(version: "18.6.0")
        }
      }
    }
  }
}
