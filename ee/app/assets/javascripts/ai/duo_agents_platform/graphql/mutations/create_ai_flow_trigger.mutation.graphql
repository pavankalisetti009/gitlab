#import "../fragments/flow_trigger.fragment.graphql"

mutation createAiFlowTrigger($input: AiFlowTriggerCreateInput!) {
  aiFlowTriggerCreate(input: $input) {
    aiFlowTrigger {
      ...FlowTriggerFragment
    }
    errors
  }
}
