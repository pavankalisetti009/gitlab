query getCatalogConsumerItems($projectId: ProjectID!, $itemTypes: [AiCatalogItemType!]) {
  aiCatalogConfiguredItems(projectId: $projectId, itemTypes: $itemTypes) {
    nodes {
      id
      item {
        id
        name
      }
    }
  }
}
