query getCatalogConsumerItems($projectId: ProjectID!) {
  aiCatalogConfiguredItems(projectId: $projectId, itemTypes: [FLOW, THIRD_PARTY_FLOW]) {
    nodes {
      id
      item {
        id
        name
      }
    }
  }
}
