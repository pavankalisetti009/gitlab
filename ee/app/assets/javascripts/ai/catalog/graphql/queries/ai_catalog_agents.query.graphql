#import "../fragments/ai_catalog_item.fragment.graphql"
#import "~/graphql_shared/fragments/page_info.fragment.graphql"

query aiCatalogAgents($before: String, $after: String, $first: Int, $last: Int) {
  aiCatalogItems(itemType: AGENT, before: $before, after: $after, first: $first, last: $last) {
    nodes {
      ...BaseAiCatalogItem
    }
    pageInfo {
      ...PageInfo
    }
  }
}
