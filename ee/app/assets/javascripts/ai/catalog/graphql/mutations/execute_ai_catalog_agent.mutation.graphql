mutation executeAiCatalogAgent($input: AiCatalogAgentExecuteInput!) {
  aiCatalogAgentExecute(input: $input) {
    errors
    workflow {
      id
      project {
        id
        fullPath
      }
    }
  }
}
