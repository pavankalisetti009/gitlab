query getNamespaceStatuses($fullPath: ID!) {
  namespace(fullPath: $fullPath) {
    id
    statuses {
      nodes {
        id
        name
        iconName
        color
        category
        description
      }
    }
  }
}
