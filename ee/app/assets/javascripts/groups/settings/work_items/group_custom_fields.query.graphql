query groupCustomFields($fullPath: ID!) {
  group(fullPath: $fullPath) {
    id
    customFields(active: true) {
      count
      nodes {
        id
        name
        fieldType
        active
        createdAt
        updatedAt
        selectOptions {
          id
          value
        }
        workItemTypes {
          id
          name
        }
      }
    }
  }
}
