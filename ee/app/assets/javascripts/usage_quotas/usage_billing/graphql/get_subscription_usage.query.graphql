query getSubscriptionUsage($namespacePath: ID) {
  subscriptionUsage(namespacePath: $namespacePath) {
    lastUpdated
    purchaseCreditsPath
    poolUsage {
      totalCredits
      creditsUsed
    }
    # overage {
    #   isAllowed
    #   creditsUsed
    # }
  }
}
