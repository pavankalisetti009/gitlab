query getSubscriptionUsage($namespacePath: ID, $startDate: ISO8601Date, $endDate: ISO8601Date) {
  subscriptionUsage(namespacePath: $namespacePath, startDate: $startDate, endDate: $endDate) {
    poolUsage {
      totalCredits
      creditsUsed
    }
  }
}
