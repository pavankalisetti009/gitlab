#import "./scan_profile.fragment.graphql"

# @feature_category: security_asset_inventories
query SecurityScanProfile($id: SecurityScanProfileID!) {
  securityScanProfile(id: $id) {
    ...ScanProfileFields
  }
}
