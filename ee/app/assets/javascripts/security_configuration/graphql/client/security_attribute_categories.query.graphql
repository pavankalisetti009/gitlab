query getSecurityAttributeCategories($fullPath: ID!) {
  group(fullPath: $fullPath) {
    id
    securityAttributeCategories @client {
      nodes {
        id
        name
        description
        multipleSelection
      }
    }
  }
}
