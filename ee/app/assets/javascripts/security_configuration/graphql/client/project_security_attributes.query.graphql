query getProjectSecurityAttributes($fullPath: ID!, $categoryId: ID) {
  project(fullPath: $fullPath) {
    id
    securityAttributes(categoryId: $categoryId) @client {
      nodes {
        id
        category {
          id
          name
        }
        name
        description
        color
        projectCount
      }
    }
  }
}
