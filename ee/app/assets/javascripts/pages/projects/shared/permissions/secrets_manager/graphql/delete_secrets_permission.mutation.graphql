# @feature_category: secrets_management

mutation deleteSecretsPermission($fullPath: ID!, $principal: PrincipalInput!) {
  secretsPermissionDelete: projectSecretsPermissionDelete(
    input: { projectPath: $fullPath, principal: $principal }
  ) {
    errors
  }
}
