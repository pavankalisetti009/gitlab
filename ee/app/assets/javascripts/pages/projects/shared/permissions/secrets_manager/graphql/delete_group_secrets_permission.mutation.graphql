# @feature_category: secrets_management

mutation deleteGroupSecretsPermission($fullPath: ID!, $principal: PrincipalInput!) {
  secretsPermissionDelete: groupSecretsPermissionDelete(
    input: { groupPath: $fullPath, principal: $principal }
  ) {
    errors
  }
}
