query getComplianceFrameworksCompactQuery($fullPath: ID!) {
  namespace(fullPath: $fullPath) {
    id
    name
    complianceFrameworks {
      nodes {
        id
        name
        default
        description
        color
      }
    }
  }
}
