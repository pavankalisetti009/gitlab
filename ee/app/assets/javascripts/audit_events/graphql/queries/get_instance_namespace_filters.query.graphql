query getInstanceNamespaceFilters($search: String) {
  groups(search: $search) {
    nodes {
      id
      name
      fullPath
    }
  }
  projects(search: $search) {
    nodes {
      id
      name
      fullPath
    }
  }
}
