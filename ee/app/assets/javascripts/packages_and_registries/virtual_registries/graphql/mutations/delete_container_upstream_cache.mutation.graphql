# @feature_category: virtual_registry

mutation containerUpstreamCacheDelete($id: VirtualRegistriesContainerUpstreamID!) {
  cacheDelete: containerUpstreamCacheDelete(input: { id: $id }) {
    errors
  }
}
