query getVirtualRegistryUpstreams($id: VirtualRegistriesPackagesMavenRegistryID!) {
  virtualRegistriesPackagesMavenRegistry(id: $id) {
    id
    upstreams {
      id
      cacheValidityHours
      metadataCacheValidityHours
      name
      url
      registryUpstreams {
        id
        position
      }
    }
  }
}
