query getMavenUpstreamCacheEntriesCount(
  $id: VirtualRegistriesPackagesMavenUpstreamID!
  $search: String
) {
  upstream: virtualRegistriesPackagesMavenUpstream(id: $id) {
    id
    cacheEntries(search: $search) {
      count
    }
  }
}
