query getMavenUpstreamsCount($groupPath: ID!, $upstreamName: String) {
  group(fullPath: $groupPath) {
    id
    upstreams: virtualRegistriesPackagesMavenUpstreams(upstreamName: $upstreamName) {
      count
    }
  }
}
