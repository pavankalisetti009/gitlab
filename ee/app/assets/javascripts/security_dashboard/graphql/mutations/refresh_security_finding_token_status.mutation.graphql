mutation refreshSecurityFindingTokenStatus($securityFindingUuid: String!) {
  refreshSecurityFindingTokenStatus(input: { securityFindingUuid: $securityFindingUuid }) {
    errors
    findingTokenStatus {
      id
      status
      createdAt
      updatedAt
      lastVerifiedAt
    }
  }
}
