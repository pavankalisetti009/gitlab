#import "./member_role.fragment.graphql"

query memberRole($id: MemberRoleID!) {
  memberRole(id: $id) {
    ...MemberRole
    createdAt
    baseAccessLevel {
      stringValue
      humanAccess
    }
    enabledPermissions {
      nodes {
        value
      }
    }
  }
}
