mutation toggleComplianceViolationNoteAwardEmoji($awardableId: AwardableID!, $name: String!) {
  awardEmojiToggle(input: { awardableId: $awardableId, name: $name }) {
    errors
    toggledOn
    awardEmoji {
      name
      user {
        id
        name
      }
    }
  }
}
