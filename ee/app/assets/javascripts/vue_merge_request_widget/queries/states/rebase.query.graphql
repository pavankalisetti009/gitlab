query rebaseQueryEE($projectPath: ID!, $iid: String!) {
  project(fullPath: $projectPath) {
    id
    allowMergeOnSkippedPipeline
    ciCdSettings {
      mergeTrainsEnabled
    }
    mergeRequest(iid: $iid) {
      id
      rebaseInProgress
      pipelines(first: 1) {
        nodes {
          id
          project {
            id
            fullPath
          }
        }
      }
    }
  }
}
