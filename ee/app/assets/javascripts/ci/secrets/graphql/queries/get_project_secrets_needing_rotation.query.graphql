query getProjectSecretsNeedingRotation($projectPath: ID!) {
  projectSecretsNeedingRotation(projectPath: $projectPath) {
    nodes {
      name
      rotationInfo {
        rotationIntervalDays
        status
        nextReminderAt
        lastReminderAt
      }
    }
  }
}
