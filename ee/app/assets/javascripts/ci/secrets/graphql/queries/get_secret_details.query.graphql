query getSecretDetails($fullPath: ID!, $name: String!) {
  projectSecret(name: $name, projectPath: $fullPath) {
    branch
    description
    environment
    metadataVersion
    name
    status
    rotationInfo {
      rotationIntervalDays
      status
      nextReminderAt
    }
  }
}
