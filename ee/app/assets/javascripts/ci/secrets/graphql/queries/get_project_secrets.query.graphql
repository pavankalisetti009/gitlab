query getProjectSecrets($projectPath: ID!, $limit: Int, $before: String, $after: String) {
  projectSecrets(projectPath: $projectPath, first: $limit, before: $before, after: $after) {
    edges {
      cursor
      node {
        branch
        description
        environment
        metadataVersion
        name
        project {
          id
        }
        rotationInfo {
          rotationIntervalDays
          status
        }
        status
      }
    }
    pageInfo {
      endCursor
      hasNextPage
      hasPreviousPage
      startCursor
    }
  }
}
