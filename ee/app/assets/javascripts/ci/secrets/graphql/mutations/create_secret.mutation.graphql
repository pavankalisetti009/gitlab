mutation createSecret($projectPath: ID!, $name: String!, $description: String!, $value: String!) {
  projectSecretCreate(
    input: { projectPath: $projectPath, name: $name, description: $description, value: $value }
  ) {
    errors
    projectSecret {
      description
      name
    }
  }
}
