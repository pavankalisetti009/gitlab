# ee/config/events/secret_detection_token_verified.yml
---
description: A secret detection token was verified and its status determined
internal_events: true
status: active
action: secret_detection_token_verified
identifiers:
  - project
  - namespace
additional_properties:
  label:
    description: The type of token detected (e.g., gitlab_personal_access_token, AWS, GCP API key)
  property:
    description: The verification status of the token (active, inactive, unknown)
product_group: secret_detection
product_categories:
  - secret_detection
milestone: '18.6'
introduced_by_url: https://gitlab.com/gitlab-org/gitlab/-/merge_requests/208599
tiers:
  - ultimate